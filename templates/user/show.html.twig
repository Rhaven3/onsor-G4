{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}

    <div class="min-h-screen flex items-center justify-center">

        <div class="w-full max-w-md bg-white rounded-2xl shadow-lg p-8">

            {# Avatar #}
            <div class="flex flex-col items-center text-center">
                    <img class="w-24 h-24 rounded-full object-cover"
                         {% if app.user.photo is defined %}
                            src="{{ asset('assets/img/profil/' ~ user.photo) }}"
                         {% else %}
                             src="{{ asset('defaultContent/default_pp_user.jpg') }}"
                         {% endif %}
                         alt="Photo de profil">

                <h1 class="mt-4 text-2xl font-bold text-gray-900">
                    {{ user.firstName }} {{ user.lastName }}
                </h1>
                <p class="text-gray-500">
                    @{{ user.username }}
                </p>

            </div>

            {# Infos #}
            <div class="mt-8 space-y-4 text-center">

                <div>
                    <p class="text-sm font-medium text-gray-500">
                        Mobile
                    </p>
                    <p class="text-gray-900">
                        {{ user.mobile }}
                    </p>
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-500">
                        Email
                    </p>
                    <p class="text-gray-900">
                        {{ user.email }}
                    </p>
                </div>
                <div>
                    <p class="text-sm font-medium text-gray-500">
                        Site
                    </p>
                    <p class="text-gray-900">
                        {{ user.site.name }}
                    </p>

                    {% if app.user == user %}
                    <div class="mt-8 flex justify-center">
                        <a href="{{ path('app_user_update', {id: user.id}) }}">
                            <button
                                type="button"
                                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 border border-blue-700 rounded"
                            >
                                Modifier
                            </button>
                        </a>
                    </div>
                    {% endif %}
                    {% if is_granted("ROLE_ADMIN") %}
                        <div class="flex flex-col sm:flex-row items-stretch sm:items-center gap-3 w-full mt-4">
                            <a href="{{ path('user_innactif', {id: user.id}) }}"
                               class="flex-1 text-center py-2.5 px-4 border border-gray-200 rounded-lg text-xs font-semibold text-gray-700 bg-white hover:bg-gray-50 hover:text-amber-600 hover:border-amber-200 active:bg-gray-100 transition-all duration-200 shadow-sm">
                                Rendre inactif
                            </a>
                            <a href="{{ path('user_delete', {id: user.id}) }}"
                               class="flex-1 text-center py-2.5 px-4 border border-gray-200 rounded-lg text-xs font-semibold text-gray-700 bg-white hover:bg-gray-50 hover:text-red-600 hover:border-red-200 active:bg-gray-100 transition-all duration-200 shadow-sm">
                                Supprimer
                            </a>

                        </div>
                    {% endif %}


                </div>
            </div>
        </div>
    </div>
{% endblock %}
